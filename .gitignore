# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
# Local store-submission credentials (from `pnpm submit:init`) — never commit.
.env.submit
.env.submit.local
# Built npm package artifacts (packages/embed) — produced by `pnpm --filter ... build`.
packages/*/dist
stats.html
stats-*.json
.wxt
web-ext.config.ts

# Test, coverage & e2e artifacts
coverage
playwright-report
test-results

# Local skills
.claude/skills/crit*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust CLI build output
cli/target
# Embed bundle vendored into the crate at publish time (built, never committed;
# Cargo.toml `include` still packages it into the published crate).
cli/embed/
